#pragma once

#include <cstdint>

namespace soupbin::detail {

inline constexpr int no_error { -1 };

inline constexpr uint32_t send_queue_size { @SOUPBIN_MAX_SEND_QUEUE@ };
inline constexpr uint32_t recv_queue_size { @SOUPBIN_MAX_RECV_QUEUE@ };

} // namespace soupbin::detail
